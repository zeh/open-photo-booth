module PhotoBooth {
	export class AppConstants {

		// Constants
		static DESIRED_CAMERA_DIMENSIONS = 480; // Desired dimensions for width and height of camera capture; will vary depending on what the camera supports
	}
}